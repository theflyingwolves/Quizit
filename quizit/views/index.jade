extends layout

block content
  h1= title
  p Welcome to #{title}
  .item.add-task
    p Test questionbase
    div.action
      form(action='/contribute', method='post')
        input(type='hidden', value=locals._csrf, name='_csrf')
        div.name
          input(type='text', name='name', placeholder='Add a new quiz')
        div.delete
          input.btn.btn-primary.btn-sm(type="submit", value='add')  
  br
  .item.add-task
    p Test challengebase POST
    div.action
      form(action='/challenges', method='post')
        input(type='hidden', value=locals._csrf, name='_csrf')
        div.name
          input(type='text', name='challengerID', placeholder='Add a new challengerID')
        div.name
          input(type='text', name='challengeeID', placeholder='Add a new challengeeID')
        div.name
          input(type='text', name='result', placeholder='Add a result')
        div.delete
          input.btn.btn-primary.btn-sm(type="submit", value='add')
  .item.add-task
    p Test challengebase GET
    div.action
      form(action='/challengeOut', method='get')
        div.name
          input(type='text', name='challengerID', placeholder='Enter challengerID')
        div.delete
          input.btn.btn-primary.btn-sm(type="submit", value='search')
    div.action
      form(action='/challengeIn', method='get')
        div.name
          input(type='text', name='challengeeID', placeholder='Enter challengeeID')
        div.delete
          input.btn.btn-primary.btn-sm(type="submit", value='search')